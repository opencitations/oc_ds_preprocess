# Workflow

These are the scripts to run on the Matilda data:

* `unify.py`: merge all the CSVs together – usually Matilda comes with several CSVs that needs to be merged and deduplicated

* `classify.py`: Classifies the citations of Matilda into different categories: matilda_matilda, matilda_redis, etc.
